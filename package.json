{
  "name": "bean-tracker-app",
  "version": "0.1.0",
  "private": true,
  "description": "BEAN TRACKER を iOS アプリとして包むための入れ物（Capacitor）。Webの中身は frontend/ をそのまま使う。",
  "scripts": {
    "app:build": "bash scripts/build-app.sh",
    "app:sync": "npm run app:build && npx cap sync ios",
    "app:ios": "npm run app:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.1",
    "@capacitor/browser": "^6.0.3",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
